language: swift
osx_image: xcode10
before_install:
  - cd Issue\ Manager
install:
  - echo 'placeholder'
script:
  - carthage bootstrap --platform iOS
  - xcodebuild clean build -sdk iphonesimulator -project Issue\ Manager.xcodeproj/ -scheme Issue\ Manager CODE_SIGNING_REQUIRED=NO
after_failure:
  - find /var/ -name carthage-xcodebuild.*.log -exec cat {} "+"
